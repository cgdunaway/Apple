// Create a watcher to look for softwareupdate??? Whatever process is run from System Preferences. OR Install macOS *

//When something is found, elevate user to admin

//Try removing admin when process is closed. If process keeps running all the way through reboot, then may need to revoke admin on next login.
